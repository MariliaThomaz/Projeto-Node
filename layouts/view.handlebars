<div id="job-view-container" class="container">
  <div class="row text-center">
    <a class="back-link main-color" href="/">Voltar</a>
    <div id="img-container" class="col-md-12">
      <img src="/img/company.png" alt="Company">
    </div>
    <div class="col-md-12">
      <h1 class="main-color">{{job.dataValues.sabor}}</h1>
      <p class="bold">Quem eles estão buscando:</p>
      <p class="job-desc">{{job.dataValues.quantidade}}</p>
      <p><span class="bold">O sabor informado é de:</span> R${{job.dataValues.valorT}}</p>
      <p>
        {{!-- Para sabor na <span class="bold main-color">{{job.dataValues.sabor}}</span> envie um e-mail para a <a class="main-color" href="mail:to{{job.dataValues.email}}">{{job.dataValues.email}}</a> --}}
      </p>
    </div>
  </div>
</div>